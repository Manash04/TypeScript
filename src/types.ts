type User ={
    firstName: String,
    lastName: String,
    age: Number
}

//The difference between type and interface is that in 
//type--> type User ={}
//interface--> interface User2 {}

interface User2 {
    firstName: String,
    lastName: String,
    age: Number
}

function isLegal(user :User){
    
}